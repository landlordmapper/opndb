[project]
name = "opndb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandera>=0.22.1",
]

[dependency-groups]
dev = [
    "ruff>=0.9.6",
]
